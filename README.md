# YouTube Reklama Skip

Chrome-расширение, которое автоматически пропускает спонсорские вставки, интро, саморекламу и другие нежелательные сегменты на YouTube с помощью Google Gemini AI.

![Chrome Manifest V3](https://img.shields.io/badge/Manifest-V3-blue)
![Vanilla JS](https://img.shields.io/badge/Vanilla-JS-yellow)
![License](https://img.shields.io/badge/license-MIT-green)

## Как это работает

1. При открытии видео расширение отправляет название и URL на анализ через **Native Messaging** → Python-хост → [`gemini-cli`](../gemini-cli/)
2. Gemini возвращает JSON с таймингами сегментов
3. Тайминги отрисовываются цветными полосками прямо на прогресс-баре YouTube
4. При достижении сегмента плеер автоматически перематывает вперёд
5. Результат кэшируется — при повторном просмотре анализ не нужен

## Типы сегментов

| Тип | Цвет | Автопропуск |
|---|---|---|
| Sponsor | 🔴 Красный | Вкл |
| Self-promo | 🟠 Оранжевый | Вкл |
| Interaction | 🟡 Жёлтый | Вкл |
| Outro | ⚫ Серый | Вкл |
| Preview | 🔵 Голубой | Выкл |
| Greeting | 🟣 Фиолетовый | Выкл |
| Chapter | 🟢 Зелёная точка | — |
| Highlight | ⭐ Золотая звезда | — |

## Установка

### 1. Зависимости

Требуется установленный [`gemini-cli`](../gemini-cli/) — Python-пакет для работы с Gemini API:

```bash
cd ../gemini-cli
pip install -e .
gemini auth   # одноразовая авторизация
```

### 2. Загрузка расширения

1. Открыть `chrome://extensions/`
2. Включить **Режим разработчика**
3. **Загрузить распакованное** → выбрать папку `youtube-reklama-skip`
4. Скопировать **ID расширения** (вида `abcdefghijklmnopqrstuvwxyzabcdef`)

### 3. Установка Native Messaging хоста

```bash
cd native_host
./install.sh <ID_расширения>
```

Скрипт создаёт манифест в `~/.config/google-chrome/NativeMessagingHosts/`.

## Архитектура

```
YouTube (youtube.js)
    → chrome.runtime.sendMessage('analyze_video_request')
    → Background (background.js)
        → проверяет кэш chrome.storage.local
        → chrome.runtime.sendNativeMessage('com.rskip.gemini', ...)
        → Native Host (gemini_host.py)
            → gemini_cli.client.ask()
            → парсит JSON из ответа
        → кэширует и рассылает всем YouTube-вкладкам
    → youtube.js рисует маркеры и автопропускает сегменты
```

## Стек

- **Vanilla JS / CSS** — без фреймворков и бандлеров
- **Chrome Manifest V3** — Service Worker, Native Messaging
- **Python** — native host (`gemini_host.py`)
- **gemini-cli** — обёртка над Gemini API

## Отладка

| Что | Где |
|---|---|
| Background / кэш | `chrome://extensions/` → Service Worker → консоль, фильтр `[RSKIP Background]` |
| YouTube-скрипт | DevTools на вкладке YouTube, фильтр `[RSKIP YouTube]` |
| Native host | Запустить `native_host/gemini_host.py` вручную, смотреть stderr |
| Кэш | `chrome.storage.local.get(null, console.log)` в консоли SW |

## Лицензия

MIT
